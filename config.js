// พาธไฟล์ข้อมูล ทั้งหมดเป็น relative path เพื่อให้ทำงานได้ทั้งบน GitHub Pages
// และตอนเปิดทดสอบในเครื่อง (ผ่าน local server)
window.APP_PATHS = {
  stationsConfig: "config/stations.json",
  history: "data/history.json",
  storageHistory: "data/storage_history.json",
};
